# Sunshine Preschool — AI Front Desk

> Brightwheel take-home project. Full README coming in M5.

## Quick start

```bash
cp .env.example .env.local   # fill in your env vars
pnpm install
npx tsx scripts/seed.ts      # seed the database
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000).
